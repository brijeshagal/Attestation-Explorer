{
  "name": "index",
  "license": "UNLICENSED",
  "scripts": {
    "codegen": "graph codegen",
    "build": "graph build",
    "deploy": "graph deploy --node https://api.studio.thegraph.com/deploy/ index",
    "create-local": "graph create --node http://localhost:8020/ index",
    "remove-local": "graph remove --node http://localhost:8020/ index",
    "deploy-local": "graph deploy --node http://localhost:8020/ --ipfs http://localhost:5001 index",
    "test": "graph test"
  },
  "dependencies": {
    "@graphprotocol/graph-cli": "0.43.0",
    "@graphprotocol/graph-ts": "0.29.1"
  },
  "devDependencies": {
    "@graphprotocol/client-cli": "^2.2.20",
    "matchstick-as": "0.5.0"
  }
}
